# 工单19测试结果

## 一、结构检查
- 目录已按 `部署 / 测试 / 设计 / 研发 / 优化` 创建。
- Python 文件已拆分为小文件，便于控制在 300 行以内。
- 所有 Python 文件均小于 300 行。
- 每个 Python 文件已写文件说明与工单编号。

## 二、接口检查
- `GET /api/health` 返回 `ok=true` 与服务名。
- `GET /api/spots/options` 正常返回地区列表、景点列表、城市与图片地址。
- `POST /api/session/create` 正常返回 `session_id`。
- `POST /api/plan/generate` 正常返回策划结果。
- `POST /api/content/generate` 正常返回传播内容结果。
- `POST /api/memorial/generate` 正常返回纪念内容结果。
- `POST /api/export/flowchart` 正常返回 Mermaid 结果。
- `POST /api/export/markdown-pack` 正常返回 Markdown 方案包下载响应。

## 三、页面与场景检查
- 首页已升级为高质感工作台，包含主视觉大图、地区选择、景点选择、景点卡片、地图路线区与结果卡片。
- 页面首次加载时，地区下拉返回 5 个地区，默认景点与主视觉同步展示。
- 真实切换地区为“西北”、景点为“兵马俑”后，城市自动切换为“西安”，主视觉标题与摘要同步更新。
- 真实点击“定位并规划”后，页面成功基于当前位置生成到“兵马俑”的高德驾车路线。
- 本轮地图验证得到的真实结果为：预计距离约 `1069.9` 公里，预计时长约 `703` 分钟，页面成功展示前 8 条路线步骤。
- 真实点击“生成策划方案”“生成传播内容”“生成路线推荐”“生成流程图”后，页面均能展示对应结果，其中策划、传播、推荐各返回 6 个结果分区，流程图返回 2 个结果分区。
- 地图与主流程综合验证截图已保存：`测试/verify-map-page.png`。
- 地图与主流程综合验证数据已保存：`测试/verify-map-result.json`。
- 第三轮页面验证截图已保存：`测试/verify-page-round3.png`。
- 第二轮页面验证截图已保存：`测试/verify-page-round2.png`。
- 首轮页面验证截图保留在：`测试/verify-page.png`。
- 已成功下载完整方案包：`测试/完整方案包.md`。
- 下载响应头验证文件已保存：`测试/markdown-pack.headers.txt`。
- 探测场景：完整方案包下载初版因中文文件名未编码导致连接中断，现已修复为标准编码响应头。
- 探测场景：地图初版因模板中高德安全配置变量名写成 `window._amapSecurityConfig` 导致返回 `INVALID_USER_SCODE`，现已修复为 `window._AMapSecurityConfig`。
- 探测场景：清空主题与城市后再次点击“生成策划方案”，页面未卡死，系统使用默认值回退并正常展示结果。
- 修复复测：定位回退逻辑曾因 `app.js` 中混入中文引号导致脚本语法错误，点击“定位并规划”无法进入定位链路；修复后已通过真实页面复测，首页脚本无报错，成功展示“当前位置 → 故宫博物院”路线，预计距离约 `4.5` 公里、预计时长约 `17` 分钟，路线步骤展示前 `8` 条，复测截图见 `测试/verify-map-page-recheck.png`，复测数据见 `测试/verify-map-result-recheck.json`。
- UI 终验复测：已移除主视觉素材内嵌文案，右侧主视觉改为“纯画面 + 下方信息卡”分层展示；同时解除首屏双列等高拉伸后，左侧主卡不再被右侧内容强行撑高。路线渲染态复测结果见 `测试/verify-ui-refresh-map-routed-final.json`，主视觉截图见 `测试/verify-ui-refresh-hero-routed-final.png`，地图区截图见 `测试/verify-ui-refresh-map-section-routed-final.png`，整屏截图见 `测试/verify-ui-refresh-map-routed-final.png`。

## 四、问题记录
- 初版前端在重复点击时不会先清空结果区，可能误把旧结果当成新结果；已修复，现会先显示“正在生成...”。
- 地图接入阶段高德安全密钥变量名大小写错误，导致路线规划失败；已修复并完成真实页面复测。
