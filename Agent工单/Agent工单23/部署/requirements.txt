# ==============================================================================
# 部署 — Research Agent 项目依赖
# ==============================================================================
# 安装方式: pip install -r requirements.txt
# ==============================================================================

# === Web 框架 ===
fastapi>=0.100.0          # FastAPI Web 框架，用于构建 HTTP API
uvicorn>=0.22.0           # ASGI 服务器，运行 FastAPI 应用

# === HTTP 请求 ===
requests>=2.28.0          # HTTP 客户端，用于 API 调用和网页抓取

# === 数据处理 ===
pydantic>=2.0.0           # 数据验证库，FastAPI 依赖

# === 可选: 搜索引擎增强 ===
# serpapi-google-search   # SerpAPI 官方 Python SDK（可选，直接用 requests 调用）
